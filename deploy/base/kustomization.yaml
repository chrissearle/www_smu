apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/instance: www-smu
    app.kubernetes.io/name: www-smu
    app.kubernetes.io/version: latest

#secretGenerator:
#  - name: regcred
#    type: kubernetes.io/dockerconfigjson
#    files:
#      - secrets/.dockerconfigjson
#  - name: tls
#    files:
#      - secrets/tls.crt
#      - secrets/tls.key

generatorOptions:
  disableNameSuffixHash: true