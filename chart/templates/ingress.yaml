apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "wwwsmu.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "wwwsmu.labels" . | nindent 4 }}
  annotations:
    traefik.www-smu.kubernetes.io/router.entrypoints: web,websecure
spec:
  rules:
    - host: www-smu.k3s.home.chrissearle.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "wwwsmu.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
  tls:
    - secretName: tls